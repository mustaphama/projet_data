version: '3.8'

services:
  reporting:
    container_name: evidently-reporting
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:8082"
    volumes:
      - ../data:/app/data
      - ./workspace:/app/workspace